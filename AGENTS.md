# Agent notes

Operational guidance for this repo — query workflow, CLI commands, parquet schema, config handling, and protobuf pitfalls — lives in [.claude/skills/deadlock-matches/SKILL.md](.claude/skills/deadlock-matches/SKILL.md). Read it before answering match questions or extending the tooling.
